name = "nushell"
version = "0.94.0"
description = "A new type of shell that works with structured data"
homepage = "https://www.nushell.sh/"
license = ["MIT"]
source = "https://github.com/nushell/nushell/archive/refs/tags/0.94.0.tar.gz"
build_system = "cargo"
depends = ["glibc"]
optional_depends = ["openssl"]
